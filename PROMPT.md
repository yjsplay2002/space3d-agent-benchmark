아래 스펙대로 웹 애플리케이션을 **현재 작업 디렉토리 바로 아래에** 구현하세요.
(`space3d/` 같은 하위 폴더를 만들지 말고, 현재 디렉토리가 곧 프로젝트 루트입니다.
즉 `./package.json`, `./index.html`, `./src/...` 가 되어야 합니다.)

작업이 끝나면 반드시 다음 두 가지가 성공해야 합니다:
  npm run build
  npm run selftest

필요한 패키지는 직접 npm install 하세요. 네트워크 사용 가능합니다.

---

# Space3D — 어린이 교육용 3D 태양계 시뮬레이터 (스펙)

## 목표
아이들(초등학생) 교육용 고해상도 3D 태양계 웹 시뮬레이터.
"SF 영화 한 장면" 같은 비주얼 — 웹사이트가 아니라 우주선 조종석 느낌.
언어: 한국어 UI.

## 기술 스택 (고정)
- Vite + vanilla JavaScript (프레임워크 없음)
- Three.js 최신 (npm 설치, `three/addons/` 경로 사용)
- 정적 빌드 → Vercel 배포 (`vite build`, output `dist/`)
- 외부 런타임 CDN 금지 — 전부 npm 번들. 텍스처는 로컬 `/public/textures/`.

## 씬 구성
1. **태양** — 고해상도 텍스처 + emissive + 커스텀 셰이더 글로우(코로나). 블룸으로 빛남.
2. **행성 8개** (수성~해왕성) — 각각 2k 이상 텍스처, 실제 자전축 기울기, 자전.
   - 지구: 야간 도시 불빛(night map) + 구름 레이어 + 대기 글로우 셰이더(프레넬).
   - 토성: 반투명 고리(텍스처 링).
   - 천왕성: 얇은 고리 + 옆으로 누운 자전축(97.8°).
3. **달** — 지구 주위 공전.
4. **소행성대** — 화성-목성 사이 InstancedMesh/Points 수천 개.
5. **배경** — 8k 은하수 스카이박스(별밭). 추가 별 파티클.
6. 거리/크기: 실제 비율은 시각적으로 불가능 → 교육용 압축 스케일(행성 크기 상대비 유지, 거리 로그 압축). 상단에 "실제 비율 아님" 안내.

### 텍스처
https://www.solarsystemscope.com/textures/ (CC BY 4.0) 에서 2k 세트 다운로드해서 `public/textures/` 에 넣기 (sun, mercury, venus, earth daymap, earth nightmap, earth clouds, moon, mars, jupiter, saturn, saturn ring, uranus, neptune, 8k stars milky way).
다운로드 실패한 것은 canvas 프로시저럴 텍스처로 폴백 생성 (빌드가 절대 깨지면 안 됨).

## 핵심: 궤도와 자전을 "흐르는 빛"으로 표현
- 각 행성 궤도 = 커스텀 셰이더 라인(TubeGeometry 또는 Line2). 궤도를 따라 밝은 빛 꼬리가 공전 방향으로 흐르는 애니메이션 (uTime 기반 그라디언트 이동, 여러 개의 빛 펄스). 행성 현재 위치 근처가 가장 밝고 뒤로 페이드.
- 선택된 행성: 적도 둘레에 자전 방향으로 흐르는 빛 링 표시 (자전 방향 시각화). 금성/천왕성 역자전도 정확히 반영.
- 전부 additive blending + 블룸에 걸리게.

## 인터랙션
- **줌 인/아웃**: 마우스 휠 / 핀치. OrbitControls + damping. 태양계 전체 뷰 ↔ 행성 표면 근접까지 부드럽게.
- **천체 클릭(라벨 클릭 포함)**: 영화적 카메라 이동 — fly-in + 이징(cubic), 도착하면 행성이 화면 좌측, 카메라가 행성을 따라다님(공전 추적). ESC 또는 "전체 보기" 버튼으로 복귀.
- **호버**: 행성 하이라이트 + 이름 라벨 강조.
- 각 천체 위에 항상 보이는 이름 라벨(CSS2DRenderer, 한국어), 거리에 따라 페이드.
- **시간 컨트롤**: 하단 바 — 일시정지/재생, 속도 슬라이더(0.1x~1000x), 현재 시뮬레이션 배속 표시.
- 유휴 상태에서도 살아있는 느낌: 카메라 미세 드리프트, 커서 움직임에 씬이 약간 반응(패럴랙스).

## 정보 패널 (우측)
천체 선택 시 우측에서 슬라이드-인 되는 반투명 글래스 패널. 데이터가 "물질화"되듯 순차 등장 애니메이션(stagger + 숫자 카운트업).
내용 (한국어, 초등학생 눈높이 + 정확한 과학 데이터):
- 이름(한글+영문), 유형, 대표 사진 대신 3D가 이미 있으므로 아이콘/이모지
- 수치 테이블: 지름, 질량, 태양까지 거리, 공전 주기, 자전 주기, 평균 온도, 위성 수, 중력(지구=1)
- "재미있는 사실" 3~4개 (예: 금성은 하루가 1년보다 길어요)
- 자전/공전 방향 설명 한 줄
데이터는 `src/data/bodies.js` 한 파일에 전부 (태양+8행성+달, 정확한 실제 값).

## 비주얼/후처리
- EffectComposer: UnrealBloomPass (태양·궤도 빛 글로우), 미세 필름 그레인(셰이더), 비네트.
- **스크린-스페이스 렌즈플레어 (추가 요청)**: 스프라이트 방식 금지. 렌더 결과에서 실제 밝은 픽셀을 threshold 로 추출하는 Chapman 방식 커스텀 패스로 구현 —
  1) bright-pass (1/4 해상도, luma threshold + 태양 스크린 위치 주변 공간 마스크로 궤도 라인 등 잡광 차단)
  2) UV 반전 고스트 사슬 4~5개 + 할로 링 + RGB 색수차 (fract 래핑 금지 — 화면 밖 소스는 클램프로 버릴 것, 안 그러면 화면 전체에 고스트 타일링됨)
  3) 분리형 가우시안 블러
  4) additive 합성 (방사형 미세 변조로 스타버스트 느낌, intensity ~0.5)
  주의: threshold 를 너무 낮추면 궤도 빛 펄스가 전부 고스트로 번져 사이키델릭해짐. 태양 코어만 추출되도록 조정.
- **파티클은 절대 기본 사각형 금지 (추가 요청)**: 소행성대·별 Points 는 캔버스 radial-gradient 원형 소프트 텍스처 map 적용.
- 시네마틱 톤매핑 (ACESFilmic), 어두운 우주 배경 (#000005 근처).
- UI 타이포: 우주/HUD 느낌 — Pretendard 또는 시스템 폰트 + letter-spacing, 네온 시안/앰버 포인트 컬러. 카드 그리드 금지, 씬에 통합된 HUD 느낌.
- 좌상단: 타이틀 "우리 태양계 탐험" + 짧은 안내 ("행성을 클릭해 보세요").
- 로딩 화면: 텍스처 로딩 진행률 표시 후 페이드아웃.

## 성능
- pixelRatio 상한 2, 텍스처 anisotropy 활성.
- 60fps 목표. 소행성대는 InstancedMesh 1개 드로우콜.
- 모바일에서도 동작(터치 컨트롤, 패널은 하단 시트로 전환은 선택사항 — 최소한 안 깨지게).

## 실제 천체 위치 (실시간 역법)

지금까지의 궤도 배치는 "보기 좋은" 임의 위상이었다. 여기서부터는 **실제
날짜의 실제 하늘**을 반영한다. 아이가 오늘 밤 창밖을 보고 확인할 수 있어야
한다는 것이 이 기능의 존재 이유다.

### 계산
- `src/ephemeris.js` 신규. 외부 천문 라이브러리 금지 — 직접 구현한다.
- J2000 기준 궤도 요소 + 세기당 변화율로 8행성의 일심 황경/황위/동경거리를
  계산(케플러 방정식은 뉴턴-랩슨 반복). 교육용이므로 **황경 오차 1° 이내**면 충분.
- 달은 지구 기준 지심 위치. 주요 섭동항(에벡션, 출차, 이각변화) 정도까지만
  반영하고 그 이상은 생략해도 된다. **황경 오차 0.5° 이내** 목표.
- 시각 기준은 UTC. 율리우스일(JD)로 변환해 계산한다.
- 씬의 압축 스케일과 무관하게 **각도(황경)는 실제값을 그대로 쓴다.** 거리만
  기존 로그 압축을 적용한다. 즉 위에서 내려다본 행성들의 배치각이 실제와 맞아야 한다.

### 날짜 컨트롤
- 하단 시간 바에 날짜 컨트롤을 추가한다: `◀ 하루 전` / `오늘` / `하루 후 ▶`.
- 현재 표시 중인 날짜를 한국어로 크게 표기 (예: `2026년 7월 27일`).
- **버튼을 누르면 즉시 반영된다.** 보간 애니메이션이나 배속 감기 금지 —
  천체가 그 자리에서 새 위치로 점프해야 "하루 사이에 이만큼 움직였구나"가 보인다.
- 기존 재생/배속 슬라이더와 공존한다. 재생 중 날짜 버튼을 누르면 그 날짜로
  점프한 뒤 거기서부터 이어서 흐른다.
- 키보드: `←` / `→` 로도 하루씩 이동.

## 지구에서 보는 달 (핵심 교육 기능)

이 시뮬레이터에서 **가장 강조되어야 할 화면**이다. 다른 행성 패널보다 크고
눈에 띄게 다룬다.

### 달 관측 뷰
- 화면 한쪽에 **항상 떠 있는 달 인셋 패널**. 지구에서 육안으로 보는 그대로의
  달을 원형으로 그린다. 3D 씬의 달을 축소한 것이 아니라, 위상 기하로 계산한
  전용 렌더(별도 캔버스 또는 작은 씬)여야 한다.
- 명암 경계선(터미네이터)이 위상각에 따라 정확히 움직인다. 초승달은 가늘게,
  보름달은 꽉 찬 원. **밝은 쪽이 향하는 방향이 태양 방향과 일치**해야 한다.
- 달 표면 텍스처를 입혀 바다(어두운 무늬)가 보이게 한다. 항상 같은 면만
  보인다는 사실(조석 고정)을 반영 — 위상이 변해도 무늬는 회전하지 않는다.
- 함께 표기: 위상 이름(한국어), 조명률 %, 월령(일), 다음 보름달까지 남은 일수.
- 위상 이름은 8구간: 삭(신월) / 초승달 / 상현달 / 차오르는 볼록달 /
  보름달 / 기우는 볼록달 / 하현달 / 그믐달.
- 날짜 컨트롤로 하루씩 넘기면 이 패널이 즉시 갱신된다. **하루씩 눌러가며
  달이 차고 기우는 것을 보는 것이 이 기능의 목적**이므로, 여기서 끊기거나
  한 박자 늦게 반영되면 안 된다.

### 3D 씬에서의 강조
- 달을 선택하면 카메라가 **지구 쪽에서 달을 바라보는 시점**으로 이동한다
  (기존 행성처럼 옆에서 보는 것이 아니라, 지구에서 본 시선 방향).
- 지구-달-태양 관계를 설명하는 보조선을 그린다: 태양광 방향 화살표, 달의
  밝은 반구 표시. 왜 그 위상으로 보이는지가 3D로 납득되어야 한다.
- 정보 패널에 "지금 달이 이렇게 보이는 이유" 한국어 설명 2~3줄을 위상에 따라
  동적으로 생성한다.

### 자체 검증 스크립트 (필수)
`npm run selftest` — Node에서 브라우저 없이 실행되는 검증 스크립트.
`src/ephemeris.js`를 직접 import 해서 아래를 assert 하고, 실패 시 exit 1.

1. 임의 기준일부터 400일간 하루 간격으로 달 위상각을 계산했을 때, 보름
   시점의 평균 간격이 **29.53일 ± 0.3일** (삭망월).
2. 같은 구간에서 지구의 일심 황경이 하루 평균 **0.9856° ± 0.02°** 전진.
3. 조명률이 항상 0~1 범위이고, 보름 시점에 0.99 초과, 삭 시점에 0.01 미만.
4. 위상각이 항상 0~360 범위이며 단조 증가(360에서 랩)한다.
5. 8행성 전부에 대해 공전 주기가 알려진 값의 **±1% 이내**로 재현된다
   (황경이 360° 도는 데 걸리는 일수로 측정).

이 스크립트는 채점 기준이다. 통과하지 못하면 역법 구현이 틀린 것이다.

## 파일 구조
```
space3d/
  index.html
  package.json  (scripts: dev, build, preview, selftest)
  vite.config.js
  public/textures/...
  src/main.js         # 부트스트랩
  src/scene.js        # 씬/카메라/렌더러/후처리
  src/bodies.js       # 행성 생성
  src/orbits.js       # 흐르는 빛 궤도 셰이더
  src/camera.js       # 시네마틱 카메라 이동/추적
  src/ephemeris.js    # 실제 천체 위치 계산 (JD, 케플러, 달 위상)
  src/moonview.js     # 지구에서 보는 달 인셋 패널
  src/ui.js           # 정보 패널, 시간/날짜 컨트롤, 라벨
  src/data/bodies.js  # 과학 데이터(한국어)
  src/style.css
  scripts/selftest.mjs
```

## 완료 기준
- `npm run build` 에러 없이 성공.
- `npm run selftest` 통과 (exit 0).
- 브라우저에서: 줌 동작, 행성 클릭 → 카메라 플라이인 + 우측 패널 데이터 표시,
  궤도에 빛 흐름 애니메이션 보임.
- 오늘 날짜의 행성 배치가 실제 하늘과 맞고, 날짜 버튼으로 하루씩 넘기면
  천체 위치와 달 위상이 즉시 갱신됨.
- 달 인셋 패널이 항상 보이고, 위상·조명률·월령이 날짜에 따라 정확히 바뀜.
