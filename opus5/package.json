{
  "name": "space3d",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "우리 태양계 탐험 — 어린이 교육용 3D 태양계 시뮬레이터",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "selftest": "node scripts/selftest.mjs",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "moon-check": "npm run build && node scripts/moon-check.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "puppeteer": "^25.3.0",
    "vite": "^8.1.5"
  }
}
